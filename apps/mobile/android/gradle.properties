# Project-wide Gradle settings.
#
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
#
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true
# Reuse daemon for faster incremental builds.
org.gradle.daemon=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Android Jetifier disabled — this project uses only AndroidX dependencies.
android.enableJetifier=false

# 允许非 ASCII 路径（Windows AGP 8.x 会对含中文字符路径硬拒绝，Linux 也保持打开无害）
android.overridePathCheck=true

# Force Gradle to use JDK 17 (AGP 8.2 requires JDK 17–20, JDK 21+ major version rejected).
# We prefer the local JDK 17 installed to $HOME/local/jdk17 (user-land, no sudo).
# Falls back to JAVA_HOME / $PATH if that path is missing (e.g. on Windows boxes).
org.gradle.java.installations.auto-detect=false
org.gradle.java.installations.auto-download=false
# Relative paths resolved against project dir are problematic, so we set this via a
# wrapper script that checks $HOME/local/jdk17 first. Keep explicit fallback unset so
# Gradle falls back to the daemon's JAVA_HOME when the pinned path is absent.
org.gradle.java.home=/home/u/local/jdk17
