# 2026-05-26 工作日志

> ✅ [M3 Onboarding 完善] 多步骤引导重写 — i18n 中英文 + 组件拆分 + 视觉升级
> ✅ [M6 Agent 剩余项] 审计日志 + 分屏工具面板 + 危险工具二次确认
> ✅ [M5 图像参数面板] 完整参数面板 + 左侧参数栏/底部主操作区布局重构
> ✅ [DOCS] 15-incomplete-status.md 更新 + 代码量统计修正
> 🔍 发现 OpenAI Dall-E image() 和消息分叉树 UI 切换已实现（文档滞后）

## M3 Onboarding 完善

- **@xiabao/state**: 新增 `onboardingStepAtom`、`onboardingProviderKindAtom`、`OnboardingProviderKind` 类型
- **@xiabao/i18n**: en-US.json + zh-CN.json 新增 `onboarding` 顶级命名空间（36 key，含 step1-5 全部文案）
- **WelcomeStep.tsx** (新建): Logo + 三条特性卡片（Database/Zap/KeyRound 图标）
- **ProviderStep.tsx** (新建): 6 个 Provider 选项（新增 Anthropic + Google），2×3 网格 + 选中态高亮
- **ApiKeyStep.tsx** (新建): Key 输入 + 各 Provider 的"获取 API Key →"外链指引 + 创建并测试连接 + 成功/失败状态卡
- **ThemeStep.tsx** (新建): 主题切换 + 实时消息气泡预览卡片
- **CompleteStep.tsx** (新建): `animate-scale-in` 庆祝动画 + 引导提示
- **StepDots.tsx** (新建): 数字圆点进度条 + 连线 + 三态（已完成/当前/未完成）
- **onboarding/index.tsx** (重写): 整合 5 步路由 + `animate-fade-in` 过渡动画
- 代码架构: 拆分 6 个子组件，Jotai atom 驱动状态

## M6 Agent 剩余项

- **audit_log**: 新建 `0007_add_audit_log.sql` migration + Drizzle schema + AuditRepo + `audit` tRPC 路由
- **agent_steps 增强**: 添加 `source`（builtin/mcp）+ `serverId` 列追踪调用来源
- **审计日志**: `executeToolCall` 内自动记录每次工具调用（runId/toolName/toolArgs/toolResult/source/durationMs/success/error）
- **危险工具确认**: `run_shell`/`file_write` 调用前弹出确认对话框，允许/拒绝，同 run 后续自动放行
- **AgentService**: 新增 `confirmTool()` 方法 + `pendingConfirmations` Map + Promise 解挂机制 + `confirm-tool` AgentEvent 类型
- **agent tRPC**: 新增 `agent.confirmTool` mutation
- **ToolPanel.tsx** (新建): 分屏工具面板 — 上半实时预览当前结果，下半完整调用历史（名称/耗时/成功失败）
- **AgentWorkspace** (重写): confirm 对话框 + split 模式下渲染 ToolPanel
- **@xiabao/core**: AgentEvent 联合类型新增 `confirm-tool` variant

## M5 图像参数面板 + 布局重构

- **image tRPC router**: `generate` subscription 新增 size/quality/n/negative/steps/seed/guidance 参数
- **ImageService**: `ImageGenerateInput` 新增 negative/steps/seed/guidance，generate 传递完整参数到 Repo
- **ImageWorkspace** (重写): 完整参数面板（尺寸/质量/数量/负面提示词/Steps/Seed/Guidance）
- **布局**: 左侧参数栏（220px，可折叠 `«`/`»`）+ 底部主操作区（提示词 + 模型选择 + 生成按钮）

## 发现

- OpenAI Dall-E `image()` 方法在 `openai.ts` L291-327 已完整实现（文档描述滞后）
- 消息分叉树 UI 切换（BranchSwitcher + UserBubbleWithSiblings/AssistantWithSiblings + gotoSibling）已完整集成（文档描述滞后）

## 文档维护

- `15-incomplete-status.md`: 更新 M2 FTS5✅、M6 Agent 工具增强、基础设施项、完成度估算、建议优先级
- 代码量统计表修正: core 27→10 tests、server 56→61、app-ui 40→44、ui 16→15

## 代码量

| 操作 | 文件数 |
| ---- | ------ |
| 新建 | 13     |
| 修改 | 14     |
| 总计 | 27     |

## M7 Web PWA

- **vite-plugin-pwa**: VitePWA 插件 + Workbox precaching + runtimeCaching（/trpc NetworkOnly / 图片字体 CacheFirst）
- **manifest.webmanifest**: PWA 安装清单（name/icons/display:standalone/theme_color:#22C55E）
- **public/icon.svg**: SVG 图标源
- **web-deploy.yml**: GitHub Actions CI — Cloudflare Pages 自动部署
- **AppShell**: `<768px` 移动端降级（全屏 + 底部 TabBar）
- **Web server**: 注入 `LibsqlVecStore` 替代 MemoryVectorStore + `createWebFileAdapter` FilePort

## Crypto + Sync 端到端加密同步

- **@xiabao/crypto**: AES-256-GCM + Argon2id + HKDF + BIP-39（argon2/@scure/bip39/@noble/ciphers—214 行完整实现）
- **DB**: `0009_add_sync.sql` — sync_state 表 + 9 主表 `rev` 字段
- **SyncRepo**: markPending/markSynced/getPending/clearResolved
- **SyncService**: configure/push/pull/LWW 冲突解决/autoSync/resetRemote
- **sync tRPC**: status/configure/push/pull/resetRemote/disable
- **SyncSettings UI**: libsql 远程配置 + 密钥输入 + 推拉按钮 + 状态显示（3 态 Loading）
- **desktop webpack**: argon2 externalize 修复（native .node 不能打包进 webpack bundle）

## UI 测试基础设施

- **@xiabao/ui**: 首次补全测试 — vitest + @testing-library/react + jsdom 配置
- 15 个基础组件全部覆盖：Button(10) / Badge(7) / Card(7) / Input(5) / Textarea(5) / Switch(4) / IconButton(3) / Skeleton(2) / Separator(2) / Dialog / DropdownMenu / Popover / ScrollArea / Tabs / Tooltip
- **51 tests 全部通过** ✅（15 组件中 49 个 render/snapshot + 2 个修复后通过）
- vitest.config.ts + setup.ts + test 脚本

## License 更新

- LICENSE: AGPL-3.0 → **CC BY-NC-SA 4.0** 完整法律文本
- LICENSE-COMMERCIAL: 已删除
- README.md: 许可证章节更新 + 项目状态全 M0-M6 勾选 + 版本升级 0.1.0-dev + Git URL 修正
- 18 个 package.json: 全部 `"CC-BY-NC-SA-4.0"`
