- [x] `image_generations` 表 schema 定义完整，字段与 docs/04-data-model.md §6 设计一致（含 `result_url` 字段支持双存储）
- [x] 迁移 SQL 文件 `0004_add_image_generations.sql` 已创建，包含 CREATE TABLE + `idx_img_created` 索引
- [x] `ChatProvider` 接口新增了 `image?()` 可选方法签名 + `ImageGenerateOptions` / `ImageGenerateResult` 类型
- [x] OpenAI Provider 实现了 `image()` 方法，能正确调用 `/v1/images/generations` API 并解析响应
- [x] OpenAI `image()` 单元测试通过（mock HttpPort，验证请求 body 和响应解析）
- [x] `ImageRepo` 实现了 create / getById / list / updateStatus 方法
- [x] `ImageService.generate()` 能创建 queued 记录，后台异步调用 Provider，下载图片，更新状态
- [x] `ImageService.list()` 按 created_at 倒序返回分页结果
- [x] `ImageService.streamStatus()` 返回 AsyncIterable 推送状态变化（用于 tRPC subscription）
- [x] `image` tRPC router 注册到 appRouter，包含 generate subscription + list query
- [x] `generate` 是 subscription（不是 mutation），返回 `ImageGenEvent` 流（queued → running → done/error）
- [x] `imageHistoryAtom` 已新增到 `@xiabao/state`，类型 `ImageGeneration[]`
- [x] `PrimaryNav` 类型新增了 `'image'` 值
- [x] IconSidebar 新增了「图像」导航入口，点击能正确切换 primaryNavAtom
- [x] `primaryNav === 'image'` 时 AppShell 右侧渲染 ImageWorkspace 组件
- [x] ImageWorkspace 包含 prompt 输入框 + 生成按钮 + 历史画廊
- [x] 画廊能通过 trpc.image.list 查询历史，通过 trpc.image.generate subscription 发起新任务并接收状态事件
- [x] `pnpm typecheck` 全项目类型通过（新增代码零错误）
- [x] `pnpm lint` 无 lint 错误（图像相关 lint 已修复）
- [x] `pnpm test` 测试全部通过（图像测试 12/12）
